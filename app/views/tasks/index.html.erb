<h1>List of tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, task_path(task) %> : <%= task.details %>
    </li>
  <% end %>
</ul>

<%= link_to 'Ajouter une tâche', tasks_new_path, class: 'btn btn-primary' %>
